Edit uploader to be drag n drop
Only ability to drag n drop at start
once image is in. Table appears for the other information.

Style like bootstrap simple appearance.

Use React???



Turns out that deployment via. AWS CLI is inefficient.
CloudFormation can automate deployments via. JSON files.
